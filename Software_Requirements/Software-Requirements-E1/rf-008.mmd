flowchart LR
    User["User (Developer / Manager)"]

    subgraph Kanban_Task_Management
        ViewBoard((View Kanban Board))
        ShowColumns((Display Columns))
        ShowTaskInfo((Display Task Info))
        MoveTask((Move Task))
        ValidateMove((Validate Movement))
        UpdateStatus((Update Status))
        SaveChanges((Save Changes))
    end

    User -->|views| ViewBoard
    ViewBoard --> ShowColumns
    ViewBoard --> ShowTaskInfo

    User -->|drag & drop| MoveTask
    MoveTask --> ValidateMove
    MoveTask --> UpdateStatus
    UpdateStatus --> SaveChanges